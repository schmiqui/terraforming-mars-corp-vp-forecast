services:
  - type: web
    name: terraforming-mars-vp
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn web.backend:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9

